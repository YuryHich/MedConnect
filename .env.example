# Скопируйте в .env и при необходимости измените значения.
# Файл .env не попадает в git.

POSTGRES_USER=medconnect
POSTGRES_PASSWORD=medconnect
POSTGRES_DB=medconnect

JWT_SECRET=medconnect-compose-secret-change-me-320601
JWT_EXPIRES_IN=1h
CORS_ORIGIN=*

PGADMIN_DEFAULT_EMAIL=admin@medconnect.by
PGADMIN_DEFAULT_PASSWORD=medconnect
